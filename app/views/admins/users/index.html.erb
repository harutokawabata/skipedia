<div class="container">
    <h3 class="midashi3">ユーザー一覧</h3>
    <div class="row">
      <div class="c-toparea">
           <p><span class="underbar">このページの機能及び注意点</span></p>
          <ul class="c-toparea-ul">
              <li>ユーザーの情報を一覧で表示しています</li>
              <li>さらに詳しい情報は会員詳細より確認できます</li>
          </ul>
      </div>
    </div>
    <div class="row">
        <table class="table table-bordered">
        	<thead>
        		<tr>
        			<th>会員ID</th>
        			<th>ユーザー名</th>
        			<th>投稿数</th>
        			<th>質問数</th>
        			<th>ステータス</th>
        			<th>編集削除</th>
        		</tr>
        	</thead>
        	<tbody>
        	    <% @users.each do |user| %>
        		<tr>
        			<th><%= user.id %></th>
        			<td><%= user.name %></td>
        			<td><%= user.posts.count %>件</td>
        			<td><%= user.questions.count %>件</td>
        			<td>
        			  <% if user.is_deleted == false %>
        			    <p>有効会員</p>
        			  <% elsif user.is_deleted == true %>
        			    <p>退会済み</p>
        			  <% end %>
        			</td>
        			<td>
        			    <%= link_to '会員詳細', admins_user_path(user.id), method: :get %> 
        			</td>
        		</tr>
        		<% end %>
        	</tbody>
        </table>
    </div>
</div>
