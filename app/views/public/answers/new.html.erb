<div class="container">
    <h3 class="midashi3">回答ページ</h3>
    <div class="row c-toparea">
      <ul class="c-toparea-ul">
        <li>ここでは質問に対して回答ができます</li>
        <li>技術論のマウンティングや誹謗中傷は禁止です</li>
        <li>回答の削除は行なえますが、編集機能はありません</li>
      </ul>
    </div>
    <div class="row question-box">
      <div class="question-box-text">
        <h4 class="question-midashi"><%= @question.tag.genre.name %>に関する質問です</h4>
        <!--<i class="far fa-question-circle"></i>-->
        <div class="tag">
        <i class="fas fa-tags"><%= @question.tag.name %></i>
        </div>
        <p><%= @question.title %></p>
      </div>
    </div>

<div class="container user-sign-in", style="margin-top: 20px;">
    <div class="row form-title">
        <h3>回答する</h3>
    </div>
    <%= alert %>
    <%= form_with model:@answer, url:answers_path, class:"form-horizontal", local:true do |f| %>
    <div class="row user-sign-in">
      <%= f.text_area :introduction, placeholder: "説明文", class:"textarea" %>
    </div>
    <div class="row user-sign-in">
      <div class="field">
        <div class="col-xs-12">
          <%= f.text_field :link, placeholder: "参考リンク" %>
          <%= f.hidden_field :question_id, :value => @question.id %>
        </div>
      </div>
    </div>
    <div class="row user-links">
        <%= f.submit "投稿する", class:"btn-lg w-80 btn_bk"%>
    </div>
   <% end %>
</div>
</div>