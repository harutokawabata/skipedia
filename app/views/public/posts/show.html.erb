<div class="container">
    <div class="row">
        <table class="table table-bordered">
        	<!--<thead>-->
        	<!--	<tr>-->
        	<!--		<th></th>-->
        	<!--		<th>名前</th>-->
        	<!--		<th>惑星</th>-->
        	<!--		<th>勝点</th>-->
        	<!--	</tr>-->
        	<!--</thead>-->
        	<tbody>
        		<tr>
        			<th class="col-xs-2">タイトル</th>
        			<td><%= @post.title %></td>
        		</tr>
        		<tr>
        			<th>ジャンル・タグ</th>
        			<td><%= @post.genre.name %> <%= @post.tag.name %></td>
        		</tr>
        		<tr>
        		    <th>内容</th>
        		    <td><%= @post.introduction %></td>
        		</tr>
        		<tr>
        		    <th>参考リンク</th>
        		    <td><%# @post.link %></td>
        		</tr>
        	</tbody>
        </table>
    </div>
    <% if @post.user == current_user %>
    <div class="row">
        <%= link_to '編集する', edit_post_path, class:"btn btn-primary" %>
        <%= link_to '削除する', post_path, method: :delete, class:"btn btn-danger" %>
    </div>
    <% end %>
</div>